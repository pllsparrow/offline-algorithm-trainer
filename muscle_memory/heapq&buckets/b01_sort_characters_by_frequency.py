# Input: one non-empty ASCII string token.
# Output: the characters grouped by non-increasing frequency; tied groups may vary.

import sys


def solve() -> None:
    text = sys.stdin.readline().rstrip("\n")

    # Compute and print the reordered string.


if __name__ == "__main__":
    solve()
