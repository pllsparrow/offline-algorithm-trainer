# Input: n, index_diff, and value_diff, then n integers.
# Output: 1 when a valid pair exists, otherwise 0.

import sys


def solve() -> None:
    n, index_diff, value_diff = map(int, sys.stdin.readline().split())
    nums = list(map(int, sys.stdin.readline().split()))

    # Compute and print 1 or 0.


if __name__ == "__main__":
    solve()
