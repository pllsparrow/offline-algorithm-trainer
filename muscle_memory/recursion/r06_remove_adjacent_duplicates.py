# Input: one line, possibly empty.
# Output: the line after collapsing every run to one character.

import sys


def solve() -> None:
    text = sys.stdin.readline().rstrip("\n")

    # Decide whether the current character survives before recursing forward.


if __name__ == "__main__":
    solve()
