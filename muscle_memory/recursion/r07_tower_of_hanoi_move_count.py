# Input: the number of disks n, where 1 <= n <= 49.
# Output: the minimum number of moves needed to transfer all disks.

import sys


def solve() -> None:
    disks = int(sys.stdin.readline())

    # Move n - 1 disks twice with one largest-disk move between them.


if __name__ == "__main__":
    solve()
