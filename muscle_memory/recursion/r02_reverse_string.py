# Input: one line, possibly empty or containing spaces.
# Output: the line reversed.

import sys


def solve() -> None:
    text = sys.stdin.readline().rstrip("\n")

    # Build the answer while recursive calls return.


if __name__ == "__main__":
    solve()
