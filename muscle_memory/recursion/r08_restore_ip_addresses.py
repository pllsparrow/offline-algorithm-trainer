# Input: one string containing 4 to 12 decimal digits.
# Output: all valid IP addresses in lexicographic order, one per line.

import sys


def solve() -> None:
    digits = sys.stdin.readline().strip()

    # Choose one valid segment at a time and stop after four segments.


if __name__ == "__main__":
    solve()
