# Input: one integer n; n may be negative.
# Output: the sum of the decimal digits of abs(n).

import sys


def solve() -> None:
    n = int(sys.stdin.readline())

    # Define a recursive helper whose argument becomes smaller on every call.


if __name__ == "__main__":
    solve()
