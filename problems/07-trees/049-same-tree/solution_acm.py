from __future__ import annotations

from support import GraphNode, ListNode, Node, RandomNode, TreeNode


class Solution:
    def is_same_tree(self, p: TreeNode[int] | None, q: TreeNode[int] | None) -> bool:
        pass


from acm_support import run_solution


if __name__ == "__main__":
    run_solution(Solution, 'is_same_tree', ['TreeNode', 'TreeNode'])
